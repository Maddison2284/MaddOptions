name = "React Router"
roles = ["frontend", "backend"]
webhooks_path = "/webhooks/app/uninstalled"

[commands]
predev = "npx prisma generate"
dev = "set PORT=3000&& npm exec -- react-router dev --host 127.0.0.1 --port 3000"